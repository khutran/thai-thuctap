<VirtualHost *:8080>
        ServerName viwebsite.vicoders.com 
        ServerAlias www.viwebsite.vicoders.com
        ServerAdmin admin@viwebsite.vicoders.com
        DocumentRoot "/var/www/viwebsite.vicoders.com/public_html"
        # VirtualDocumentRoot /var/www/html/%0
        
        <Proxy "fcgi://172.22.0.2/" enablereuse=on max=10>
                ProxySet timeout=1800
        </Proxy>

        <FilesMatch "\.php$">
        # Note: The only part that varies is /path/to/app.sock
                SetHandler "proxy:fcgi://172.22.0.2:9000"

        </FilesMatch>

        # <Directory /var/www/viwebsite.vicoders.com/>
        #         Options Indexes FollowSymlinks MultiViews
        #         AllowOverride All
        #         Require all granted
        # </Directory>

        # <IfModule mod_ruid2.c>
        #         RMode config
        #         RUidGid 33 tape
        #         RGroups apache
        # </IfModule>

        # <IfModule itk.c>
        #         AssignUserID 33 tape
        # </IfModule>

        ErrorLog /etc/httpd/logs/viwebsite.vicoders-error.log 
        CustomLog /etc/httpd/logs/viwebsite.vicoders-access.log combined
</VirtualHost>
